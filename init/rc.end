#!/bin/bash
# File run after all processes have been terminated except for PID 1

# Disable swap and remount the root as readonly
echo -- swapp off, remount ro
swapoff -a
mount -o remount,ro /

echo -- unmount
umount -a
